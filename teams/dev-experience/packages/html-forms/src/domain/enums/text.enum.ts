export enum TextType {
  TEXT = 'text',
  EMAIL = 'email',
  PASSWORD = 'password',
}

export enum InputAutoCapitalize {
  NONE = 'none',
  OFF = 'off',
  ON = 'on',
  SENTENCES = 'sentences',
  WORDS = 'words',
  CHARACTERS = 'characters',
}
